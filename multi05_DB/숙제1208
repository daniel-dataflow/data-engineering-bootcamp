status: 'A' 정상
status: 'P' 준비중
status: 'D' 단종

db.inventory.find({},{"_id":0}).pretty();


1. _id가 나오지 않게 하고 수량이 50개 이상 수량이 많은 순서대로 나오게 해라.
db.inventory.find({"qty":{$gt:50}},{"_id":0}).sort({"qty":-1}); 
2. 
db.inventory.find({$or:
                    [
                        {"status":"A"},
...                         {"status":"p"}
                    ]
                });

db.inventory.find({
    $or: [
        { "status": "A" },          // 첫 번째 조건 객체
        { "size.w": { $gt: 50 } }   // 두 번째 조건 객체
    ]
});